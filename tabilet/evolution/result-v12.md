# Authenticated authoring qualification result

Browsertools scopes candidates to one observation generation and re-resolves
their current semantic identity immediately before focus or click. Reduced
observations now include the complete portable context inventory; popup/frame
identity, origin, parentage, uniqueness, and attachment are continuously
reconciled. Closed phases prevent post-completion action, and every action
invalidates stale goal and human-completion evidence.

Actual completed-response body sizes—including chunked bodies without a
`Content-Length` header—count against the session budget. Authentication
success is bound to the first reviewed dashboard observation, while the
capability candidate retains the ordered post-login exploration and terminates
in its own goal presence assertion.

Qualification uses named seams rather than compatible-looking fixtures: a real
Browsertools envelope crosses OpenUdon validation, and a real producer profile
pair crosses Udon into Browserdriver v3. Exact UWS 1.8-aware revisions are
pinned at UWS `c9665a61adfb` and Browsertools `53a1502b75b9`. Both commits are
published, and a fresh module cache resolved their exact GitHub origins with
global/system Git configuration disabled and no URL mapping or local
replacement.
