# Prompt V4 - Additive Browser Authentication Profiles

Add portable, reviewable sign-in recipes as a separate UWS artifact while
preserving `uws.browser.1.5`. Browsertools should provide offline typed
validation, deterministic drafting, review, expiry, and local discovery, but
must not execute authentication, resolve credentials, retain sessions, or
publish login topology through the static registry.
