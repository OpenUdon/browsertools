# Prompt V8 - Manual Authentication, Value-Free Evidence

Let an operator prove explicitly authored sign-in and MFA alternatives in a
headed browser without giving Browsertools credentials, browser actions, page
content, or session state. Require exact origin and mutation authority before
contact, destroy every context, and retain only local reviewable shape facts.
