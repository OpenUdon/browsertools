# Authenticated authoring qualification hardening

Close the cross-repository authenticated-authoring review findings without
widening the live protocol or exporting browser state. Make candidate authority,
context discovery, response accounting, phase transitions, authentication
proof, and goal proof current and fail-closed. Preserve the entire reviewed
post-login exploration in the capability candidate and prove a real
Browsertools envelope across OpenUdon consumption and Udon/Browserdriver replay.

Retain existing UWS and driver compatibility, use the oldest sufficient profile
versions, keep default tests browser-free, and record exact pre-publication
dependency revisions rather than silently skipping unavailable features.
